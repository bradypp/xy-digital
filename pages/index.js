import Head from 'next/head';
import { Layout } from 'components';

const Home = () => {
    return (
        <Layout>
            <Head>
                <title>Digital Agency</title>
            </Head>
            <h1>Hello</h1>
        </Layout>
    );
};

export default Home;
