import Head from 'next/head';
import { Layout } from 'components';

const Home = () => {
    return (
        <Layout>
            <Head>
                <title>Digital Agency</title>
            </Head>
        </Layout>
    );
};

export default Home;
